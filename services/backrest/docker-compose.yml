services:
  backrest:
    image: garethgeorge/backrest:latest
    container_name: backrest
    hostname: backrest
    restart: unless-stopped
    environment:
      - BACKREST_DATA=/data
      - BACKREST_CONFIG=/config/config.json
      - XDG_CACHE_HOME=/cache
      - TMPDIR=/tmp
      - TZ=${TZ:-Europe/Bratislava}
    volumes:
      # Backrest internal
      - ${APPDATA_PATH:-/opt/homelab/appdata}/backrest/data:/data
      - ${APPDATA_PATH:-/opt/homelab/appdata}/backrest/config:/config
      - ${APPDATA_PATH:-/opt/homelab/appdata}/backrest/cache:/cache
      - ${APPDATA_PATH:-/opt/homelab/appdata}/backrest/tmp:/tmp
      # Rclone config from host
      - ${HOME}/.config/rclone:/root/.config/rclone:ro
      # Directories to backup (read-only)
      - ${APPDATA_PATH:-/opt/homelab/appdata}:/backup/appdata:ro
      - ${DATA_PATH:-/mnt/data}/users:/backup/users:ro
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    networks:
      - traefik-net
    labels:
      # Traefik routing - HTTP (local)
      - traefik.enable=true
      - traefik.http.routers.backrest.rule=Host(`backup.${LOCAL_DOMAIN:-home.local}`)
      - traefik.http.routers.backrest.entrypoints=web
      - traefik.http.services.backrest.loadbalancer.server.port=9898
      # Traefik routing - HTTPS (local via split-DNS)
      - traefik.http.routers.backrest-secure.rule=Host(`backup.${EXTERNAL_DOMAIN:-1218217.xyz}`)
      - traefik.http.routers.backrest-secure.entrypoints=websecure
      - traefik.http.routers.backrest-secure.tls=true
      - traefik.http.routers.backrest-secure.tls.certresolver=cloudflare
      - traefik.http.routers.backrest-secure.middlewares=authelia@docker
      - traefik.http.routers.backrest-secure.service=backrest
      # Homepage
      - homepage.group=Infrastructure
      - homepage.name=Backrest
      - homepage.icon=backrest
      - homepage.href=https://backup.${EXTERNAL_DOMAIN:-1218217.xyz}
      - homepage.description=Backup Management

networks:
  traefik-net:
    external: true
